export interface Settings {
      horizontalFlip: boolean;
      verticalFlip: boolean;
      speed: 'slow' | 'medium' | 'fast';
      font: string;
      fontSize: number;
      theme: 'light' | 'dark';
    }
